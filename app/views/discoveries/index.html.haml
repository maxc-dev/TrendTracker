%h1 Listing discoveries

%p{id: 'display' }
  Dummy

%h1
  = @all_trend_data.size

:javascript
  document.getElementById('display').innerHTML = "testing"

  var rails_source = new EventSource('/discoveries/events');

  rails_source.addEventListener('location', (function (evt) {
    console.log("Hi")
    console.log(evt.data)
    document.getElementById('display').innerHTML = evt.data
   }));



%table
  %thead
    %tr
      %th Country
      %th City
      %th Trend

  %tbody
    - @all_trend_data.each do |discovery|
      %tr
        %td= discovery[0]
        %td= discovery[1]
        %td= discovery[2]
